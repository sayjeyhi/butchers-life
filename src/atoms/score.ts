import { atom } from 'jotai';

export const timeAtom = atom(0);
export const livesAtom = atom(300);

export const achievedAwardAtom = atom(0);
export const achievedCoinsAtom = atom(0);
export const achievedMeatsAtom = atom(0);
export const achievedKnifesAtom = atom(0);
